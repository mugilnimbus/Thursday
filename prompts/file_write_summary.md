Summarize the file content below for future agent context in {summary_words} words.

Return a detailed but compact brief with these sections:

- Purpose: what this file is for.
- Main components: important classes, functions, UI parts, routes, or modules.
- Inputs: data, user actions, command arguments, environment variables, files, APIs, or events it expects.
- Outputs: files, UI, network responses, command output, side effects, or exported values it produces.
- Dependencies: libraries, frameworks, browser APIs, or services used.
- Important behavior: notable control flow, validation, error handling, or integration details.

Do not rewrite the code.
Do not include the full source.
Keep enough detail that another model can continue editing the project without seeing the whole file.

File path: {path}

File content:
```text
{content}
```
